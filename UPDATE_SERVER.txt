SERVER UPDATE COMMANDS
=====================

SSH into your server, then run these commands:

cd ~/tool-thinker
git pull origin main
npm install
npm run build
pm2 restart tool-thinker
pm2 status

That's it! Your server will be updated with all the new visual enhancements and professional icons.

